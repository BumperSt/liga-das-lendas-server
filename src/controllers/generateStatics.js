const matchModel = require('../models/matchModel')


const generateStatics = () => {
    getStaticByChampId = (request, response) => {

        let {champName} = request.body

    }
}

module.exports = generateStatics